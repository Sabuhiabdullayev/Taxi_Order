<nav class="navbar navbar-expand-lg bg-white navbar-light shadow sticky-top p-0">
	<a href="index.html" class="navbar-brand d-flex align-items-center px-4 px-lg-5">
		<h2 class="m-0 text-primary"><i class="fa fa-car me-3"></i>Taksi.az</h2>
	</a>
	<button type="button" class="navbar-toggler me-4" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
		<span class="navbar-toggler-icon"></span>
	</button>
	<div class="collapse navbar-collapse" id="navbarCollapse">
		<div class="navbar-nav ms-auto p-4 p-lg-0">
			<a href="/Order/Mate" class="nav-item nav-link">Ana Səyfə</a>
			<a href="/Order/Mate" class="nav-item nav-link">Elan Axdarın</a>
			@if (User.Identity.IsAuthenticated)
			{
							<a href="/Member/Order/MetaAdd" class="nav-item nav-link">Elan Yerləşdir</a>
				}
			<a href="/ContactUs/Contact" class="nav-item nav-link">Elaqe</a>
		@if (User.Identity.IsAuthenticated)
			{
				@if (User.IsInRole("Admin") || User.IsInRole("Moderator"))
				{
					<a href="/Admin/Order/AllList" class="nav-item nav-link">Admin Panel</a>
				}
			}
			@if (User.Identity.IsAuthenticated)
			{
				<a href="/Login/Logout" class="nav-item nav-link">Çıxış</a>
			}
		</div>
		@if (User.Identity.IsAuthenticated)
		{
			@await Component.InvokeAsync("_LayoutAuthenticatedUser")
		}
		else
		{
			<a href="/Login/SignIn" class="btn btn-primary py-4 px-lg-5 d-none d-lg-block"><i class="fa fa-user me-3"></i> Giris<i class="fa fa-arrow-right ms-3"></i></a>
		}

	</div>
</nav>